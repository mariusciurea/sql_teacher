# https://docs.docker.com/engine/install/ubuntu/

- name: Instaleaza Docker pe serverele Ubuntu
  hosts: all
  become: true

  tasks:
    - name: Instaleaza pachete necesare
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
        state: present
        update_cache: yes

    - name: Adauga cheia GPG Docker
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: Adauga repository Docker
      apt_repository:
        repo: deb https://download.docker.com/linux/ubuntu jammy stable
        state: present

    - name: Instaleaza Docker CE
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
        state: present
        update_cache: yes

    - name: Adauga userul ansible in grupul docker
      user:
        name: ansible
        groups: docker
        append: yes

    - name: Porneste serviciul Docker
      service:
        name: docker
        state: started
        enabled: true

    - name: Instaleaza Docker Compose
      apt:
        name: docker-compose-plugin
        state: present